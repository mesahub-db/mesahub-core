name: Docker Release

# Triggered by a version tag on mesahub-core (e.g. v1.2.3).
# Publishes the public mesahub-core image (Go API + admin UI).
# The core repo is public and self-contained -- no GH_PAT or mono checkout needed.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: mesahub-db
  IMAGE_SOURCE: https://github.com/mesahub-db/mesahub-core

jobs:
  build-core:
    name: Build mesahub-core
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout mesahub-core
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Log in to ghcr.io
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        with:
          driver: docker-container

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/mesahub-core
          tags: |
            type=semver,pattern=v{{version}}
            type=raw,value=latest
          labels: |
            org.opencontainers.image.source=${{ env.IMAGE_SOURCE }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build and push mesahub-core
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: |
            type=gha,scope=mesahub-core
            type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/mesahub-core:latest
          cache-to: type=gha,mode=max,scope=mesahub-core
          build-args: |
            NEXT_PUBLIC_ENABLE_FILE_STORAGE=true
